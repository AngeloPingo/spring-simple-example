server:
  port : 80
  tomcat:
    max-threads: 3
logging:
  level:
    pt:
      angelopingo:
        examples: DEBUG

app:
  encoding: UTF-8
  big-query:
    dataset: default-dataset-example
    table: default-table-example
